# Rules Package

Shared rule artifacts used by both `apps/web` and `apps/bot`.

## Files

- `xp_costs.json`: canonical XP spend formula definitions.

## Usage

- Web: loaded by `apps/web/app/xp_rules.py`
- Bot: loaded by `apps/bot/src/xpRules.ts`
